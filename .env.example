# Database Configuration
# Required: PostgreSQL connection string for your cloud database
# Get one from: Neon (https://neon.tech), Supabase (https://supabase.com), or Azure PostgreSQL
DATABASE_URL=postgresql://user:password@host:port/database

# Frontend API URL
# For Vercel deployment, use relative path to enable same-origin requests
# Local development: http://127.0.0.1:8000
# Production (Vercel): /api
NEXT_PUBLIC_API_URL=/api

# Optional: CORS Configuration
# Comma-separated list of allowed origins
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000,https://your-app.vercel.app

# Optional: Python Runtime
# Vercel will use this version if specified
PYTHON_VERSION=3.9

# Optional: Migration Control
# Set to true if you want to run migrations on startup (not recommended for serverless)
RUN_MIGRATIONS=false

# Auto-set by Vercel (no need to configure)
# VERCEL=1
